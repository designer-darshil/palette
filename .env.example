# PaletteParadise / KROMA — Production Environment Configuration Template
# Copy this file to .env.local for local overrides.
# NEVER commit .env or secrets to version control.

# Analytics (Optional)
VITE_GTM_ID=
VITE_GA_ID=

# Serverless API Authentication Secret (For POST /api/maintenance)
# Required in production serverless environments (e.g. Vercel Project Settings)
ADMIN_API_SECRET=
